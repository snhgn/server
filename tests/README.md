# tests/ — 临时测试脚本

存放开发过程中的临时验证脚本、调试工具、一次性测试代码。

## 使用规则

- 临时验证脚本放这里，不要污染项目根目录
- 脚本命名建议：`tmp_<功能>.py` / `check_<目标>.sh` / `verify_<流程>.py`
- 已固化进 CI 或正式测试的脚本应移到对应模块的 `tests/` 目录

## 现有脚本

| 脚本 | 用途 |
|------|------|
| `tmp_check_routes.sh` | 验证 SPA 路由 + 登录 + API 全链路（在服务器上执行） |
