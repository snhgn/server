# 核心依赖
requests>=2.28
numpy>=1.21
opencv-python-headless>=4.5
Pillow>=9.0

# 可选：CNN 兜底识别器（train.py 训练 + recognize.py 加载）
# torch>=1.12

# 可选：OCR 兜底（二选一或都装，recognize.py 会自动探测）
# ddddocr>=1.4
# pytesseract>=0.3.10   (另需系统安装 tesseract-ocr: sudo apt install tesseract-ocr)
